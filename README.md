Just a common model which is used to predict whether the person contains diabetes or not based on some of the features of their test report.
This model uses SVM algorithm and predict the diabetes rate.
